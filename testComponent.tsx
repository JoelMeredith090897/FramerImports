const TestComponent = () => {
  return (
    <div
      style={{
        width: 200,
        height: 200,
        borderRadius: 30,
        backgroundColor: "orange",
      }}
    />
  );
};

export default TestComponent;
